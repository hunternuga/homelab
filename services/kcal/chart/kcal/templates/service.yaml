apiVersion: v1
kind: Service
metadata:
  name: {{ include "kcal.fullname" . }}
  labels:
    {{- include "kcal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kcal.selectorLabels" . | nindent 4 }}
